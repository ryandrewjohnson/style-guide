'use strict';

angular.module('module.config', [])
    .config(function () {
        // main app config
    })
    .constant('API_BASE_URL', 'API URL goes here')
    .constant('Modernizr', Modernizr)
    .constant('EVENTS', {
    })
    .constant('LOCAL_STORAGE_KEYS', {
    });
